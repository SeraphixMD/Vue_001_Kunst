/**
 * Vitest browser-mode setup file.
 *
 * Imports the test CSS entry which loads Tailwind + the library's tokens,
 * base layer, and component source scanning. This makes component tests
 * render with real styles (not naked class attributes against white).
 */
import './setup.css'
